// FILE: firestore.rules
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Block all direct client read/write
    match /{document=**} {
      allow read, write: if false;
    }
  }
}