// FILE: storage.rules
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    
    // Deny all client read/write — images are made public explicitly by Functions.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}